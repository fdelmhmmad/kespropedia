<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kespropedia">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SoalActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Kuis1Fragment"
            android:label="Kuis Activity"></activity>
        <activity
            android:name=".GameWon"
            android:label="Won Activity"></activity>
        <activity
            android:name=".PlayAgain"
            android:label="Play Again Activity"></activity>
        <activity
            android:name=".Time_Up"
            android:label="Time Up Activity"></activity>
        <activity
            android:name=".TriviaQuestion"
            android:label="Trivia Activity"></activity>
    </application>

</manifest>